import { MarkdownTextPrimitive } from "@assistant-ui/react-markdown";
import type { FC } from "react";

export const MarkdownText: FC = () => {
  return (
    <MarkdownTextPrimitive className="aui-markdown-text" />
  );
};